manifest:
  remotes:
    - name: darknao
      url-base: https://github.com/darknao
  projects:
    - name: zmk
      remote: darknao
      revision: rgb-layer-24.12
      import: app/west.yml
  self:
    path: config
```

Worth double-checking that the branch actually exists before pushing — you can verify at `https://github.com/darknao/zmk/branches` or with:
```
git ls-remote https://github.com/darknao/zmk | grep rgb-layer
